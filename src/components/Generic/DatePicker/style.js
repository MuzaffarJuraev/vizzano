import styled from "styled-components";

export const Wrapper = styled.div`
  display: flex;
  align-items: center;
  column-gap: 40px;
`;

Wrapper.DateTitle = styled.p`
  font-weight: 500;
  font-size: 30px;
  margin: 0;
  cursor: pointer;
`;

export const iconStyle = {
  fontSize: "30px",
};
